seed: 42
batch_size: 256
log_interval: 10
eval_interval: 100

data:
  max_digits: 3
  eval_split: 0.2

model:
  n_layers: 6
  d_model: 384
  d_ffw: 1152
  n_heads: 8
  n_kv: 2
  vocab_size: 12 # digits 0-9, '+', '='

optimizer:
  _target_: optax.adamw
  learning_rate: 3e-4
